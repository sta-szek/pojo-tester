language: java
sudo: required
jdk:
  - oraclejdk8
after_success:
  - curl http://www.jpm4j.org/install/script > jpmInstall.sh
  - sh jpmInstall.sh
  - jpm install com.codacy:codacy-coverage-reporter:assembly
  - codacy-coverage-reporter -l Java -r build/reports/test/jacocoTestReport.xml